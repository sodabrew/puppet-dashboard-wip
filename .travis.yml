language: ruby
branches:
  only:
    - rails3
rvm:
  - "1.8.7"
  - "1.9.3"
bundler_args: --without racer
before_script:
  - cp config/database.yml.travis config/database.yml
  - bundle exec rake db:schema:load
  - bundle exec rake db:migrate
script:
  - bundle exec rake spec
